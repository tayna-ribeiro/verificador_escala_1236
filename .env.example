# Copie este arquivo para .env e preencha com seus dados reais
# cp .env.example .env

# Token do bot gerado pelo @BotFather no Telegram
BOT_TOKEN=seu_token_aqui

# Segredo para validar que as requisições webhook vieram realmente do Telegram
# Pode ser qualquer string aleatória (ex.: use: python3 -c "import secrets; print(secrets.token_hex(32))")
WEBHOOK_SECRET=seu_webhook_secret_aqui
